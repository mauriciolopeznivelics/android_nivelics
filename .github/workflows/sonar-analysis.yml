# SonarCloud Analysis Workflow
name: SonarCloud Analysis

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Shallow clones should be disabled for better analysis
    
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      continue-on-error: true  # Allow workflow to continue even if Quality Gate fails
    
    - name: Quality Gate Status
      run: |
        echo "🔍 SonarCloud analysis completed!"
        echo "📊 View detailed results at: https://sonarcloud.io/dashboard?id=mauriciolopeznivelics_android_nivelics"
        echo "⚠️  Note: Quality Gate may have failed - check SonarCloud for details"